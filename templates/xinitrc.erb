#!/usr/bin/sh

xset -dpms
xset s off
xset s noblank

URL='<%= @url %>'

firefox --kiosk "$URL" &

until xdotool search --name ".*Mozilla Firefox" windowsize <%= @resolution %> ; do
  sleep 1
done

wait
